# Environment variables for Self-Service Kiosk deployment
# Copy this file to .env.local for local overrides

# Domain configuration
DOMAIN=self-service-kiosk.givemebug.online

# API endpoints (webhook URLs)
VITE_API_BASE_URL=http://spec-extractor-app-api.givemebug.online/api

# Docker registry configuration
REGISTRY=registry.gitlab.com/givemebug/self-service-kiosk

# Container configuration
CONTAINER_NAME=self-service-kiosk
SERVICE_NAME=kiosk-ui

# SSL and reverse proxy configuration
LETSENCRYPT_HOST=self-service-kiosk.givemebug.online
LETSENCRYPT_EMAIL=nungpothi.p@gmail.com
VIRTUAL_HOST=self-service-kiosk.givemebug.online
VIRTUAL_PORT=80

# Development overrides (uncomment for local development)
# DOMAIN=localhost
# VITE_API_BASE_URL=http://localhost:3001/api
# VIRTUAL_HOST=localhost
# VIRTUAL_PORT=80